import { EntityStatus, sample } from '~/test';

export interface Sample {
  title: string;
}

console.log('Hello, World!');
console.log('This is horcruxx sdk js v0.1.0!');
console.log(sample);
console.log(EntityStatus.Active);
